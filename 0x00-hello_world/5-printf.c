#include <stdio.h>

/**
 * main - the main function is constant ate the beginning of a code
 *
 * Return: this will return 0 when the code is executed
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
