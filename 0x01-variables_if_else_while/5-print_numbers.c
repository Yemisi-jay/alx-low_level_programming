#include <stdio.h>

/**
 * main - this is constant in the code
 *
 * Return: this will return 0 when the code is executed
 */

int main(void)
{
	int a;

	for (a = 0 ; a < 10 ; a++)
		printf("%d", a);
	printf("\n");
	return (0);

}
