#include <stdio.h>

/**
 * main - this the the main function
 *
 * Return: this should return 0 when the code runs sucessfully
 */

int main(void)
{
	char i;

	for (i = 'a' ; i <= 'z' ; i++)
		putchar (i);
	putchar ('\n');
	return (0);
}
